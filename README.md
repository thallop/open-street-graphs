```
- enrich_nodes_with_embeddings.py → extract and enrich OSM graphs with satellite embeddings
- build_pyg_graphs.py → clean graphs and compute geometric features
- generate_dataloaders.py → build local k-hop subgraph datasets
- train_node_baseline.py → train a node-level baseline (MLP)
- train_egocentric_gcn.py → train a graph convolutional network 
```
